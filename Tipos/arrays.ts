(()=>{


    const prices: number[] = [12, 23, 54, 10, 4];

    const products = ['boligrafo', 'patatas', 'chuches', 'frutas' , 'bebidas'];
    
    products.forEach( v => console.log(v.toUpperCase() ) );

    const mixedData: (number | string | boolean )[] = [1, 'Juan Manuel Cano Camacho', true]




})()